# Google Cloud Translation API Integration

## ⚠️ Решение проблемы: "PERMISSION_DENIED (403)"

Если в консоли браузера видите ошибку:
```
Method doesn't allow unregistered callers (callers without established identity).
status: "PERMISSION_DENIED"
```

**Решение:**

1. **Включите биллинг** в Google Cloud Console:
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
   - Выберите ваш проект
   - Меню → **Billing** (Биллинг)
   - Нажмите **Link Billing Account** (Привязать счет)
   - Добавьте способ оплаты (даже для free tier — требуется для работы API)

2. **Убедитесь, что Cloud Translation API включена:**
   - Меню → **APIs & Services** → **Enabled APIs & services**
   - Найдите "Cloud Translation API"
   - Если её нет — нажмите **Enable APIs and Services** и найдите/включите "Cloud Translation API"

3. **Проверьте API ключ:**
   - Меню → **APIs & Services** → **Credentials**
   - Найдите ваш **API Key**
   - Скопируйте его в `.env.local`:
     ```env
     VITE_GOOGLE_TRANSLATE_KEY=AIzaSy...
     ```

---

## Быстрый старт

### 1. Получить API ключ Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. **Создайте новый проект** (или используйте существующий):
   - Нажмите на проект в левом верхнем углу
   - **New Project** → Введите имя → **Create**

3. **Включите Cloud Translation API**:
   - Меню → **APIs & Services** → **Enable APIs and Services**
   - Поиск: "Cloud Translation API"
   - Нажмите **Enable** (Включить)

4. **Включите биллинг** (обязательно!):
   - Меню → **Billing**
   - **Link Billing Account** или **Create Billing Account**
   - Добавьте способ оплаты

5. **Создайте API Key**:
   - Меню → **APIs & Services** → **Credentials**
   - Нажмите **+ Create Credentials** → **API Key**
   - Скопируйте ключ

### 2. Установить переменную окружения

Создайте файл `.env.local` в папке `my-app/`:

```env
VITE_GOOGLE_TRANSLATE_KEY=your_api_key_here
```

Пример:
```env
VITE_GOOGLE_TRANSLATE_KEY=AIzaSyD7K6R8j9Qw2k5e4r3t2y1u0i9o8p7l6m5n
```

### 3. Запустить приложение

```bash
cd my-app
npm run dev
```

Откройте http://localhost:5174 в браузере.

---

## Использование

1. **Загрузите файл** (аудио, видео, изображение) → текст будет извлечен и автоматически переведен на:
   - **Қазақша** (Казахский)
   - **Русский**
   - **English** (Английский)

2. **Или введите текст вручную** в поле "Текст для обработки" и нажмите "Перевести"

3. **Переводы появятся** в трёх карточках ниже с кнопками "Копировать"

---

## Поддерживаемые языки

Google Cloud Translation поддерживает 100+ языков. Для использования других языков отредактируйте функцию `translateToLanguages()` в `src/App.tsx`:

Найдите строки вроде `target: "kk"` и замените коды языков на нужные:
- `fr` — французский
- `es` — испанский
- `de` — немецкий
- `zh-CN` — китайский (упрощённый)
- `ar` — арабский
- `ja` — японский
- И т.д.

[Полный список кодов языков](https://cloud.google.com/translate/docs/languages)

---

## 💰 Цены

- **Бесплатный уровень**: первые 500,000 символов в месяц
- **Платный**: $15 за 1 млн символов (после лимита)

[Подробнее о ценах](https://cloud.google.com/translate/pricing)

---

## 🔍 Обработка ошибок и отладка

### Ошибка: "API key not configured"
- Убедитесь, что файл `.env.local` существует
- Проверьте, что ключ скопирован правильно
- Перезагрузите браузер (F5)

### Ошибка: "PERMISSION_DENIED (403)"
- **Включите биллинг** (см. выше)
- Убедитесь, что Cloud Translation API **включена**
- Подождите 1-2 минуты после включения API

### Ошибка: "Invalid API key"
- Проверьте, что API ключ правильный
- Создайте новый ключ, если не уверены

### Консоль браузера (F12 → Console)
Там будут полезные логи:
```
Google Cloud Translation API key not configured
Google Translate API error: ...
⚠️ Ошибка доступа! Убедитесь, что: 1. Биллинг включен...
```

---

## 🔐 Безопасность

⚠️ **Важно**: 
- Никогда не коммитьте файл `.env.local` в Git
- Убедитесь, что `.env.local` добавлен в `.gitignore`
- В production используйте сервер-сайд прокси вместо публичного ключа

### Production setup (опционально)

Для защиты API ключа в production:

1. Создайте backend endpoint на вашем сервере:
   ```javascript
   // backend/routes/translate.js
   app.post('/api/translate', async (req, res) => {
     const { text, targets } = req.body;
     const apiKey = process.env.GOOGLE_TRANSLATE_KEY; // на сервере!
     
     // Вызов Google API со стороны сервера
     // ...
   });
   ```

2. Обновите frontend для вызова своего backend вместо Google API

---

## 📚 Поддержка и ресурсы

- [Google Cloud Translation документация](https://cloud.google.com/translate/docs/reference/rest)
- [API explorer](https://cloud.google.com/translate/docs/reference/rest/v2/translate)
- [Список языков](https://cloud.google.com/translate/docs/languages)

